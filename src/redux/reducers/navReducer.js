import * as CONST from "../constants";

const initialState = {
  selectionState: "all",
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
